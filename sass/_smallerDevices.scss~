/* file: _smallerDevices (below 958px) */


// the variables are found in _globals.scss.

/*  RESPONSIVE MENU */
@media screen and (max-width: 958px) {

	body {
		font-size: 200%;
	}

	header h1 a {
		text-align: center;
		display: block;
		top: 10px;
		font-size: 20px;
		background: transparentize($blue, .6);
	}

	header h1 a:hover {
		color: transparentize(white, .2);
		text-decoration:none;
	}
	header h2 {
		visibility: hidden;
	}

.menu-dawn-container {
	background-color: $menuBackground;

	ul {
		padding: 0;
	    list-style-type: none;
	}
}

	#menu-dawn {
		background-color: $menuBackground;	 

		li {
			display: block;
		}

		li a {
			color: $menuColor;
		}

		li:nth-child(odd) {
			background-color: black;
		}

		li:nth-child(even) {
			background-color: mix($menuAltBackground, #000, 30%);
		}


		ul li a {
			color: $menuColor;
		}
	}

} // device ends here

